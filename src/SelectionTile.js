import React, { useEffect, useState } from "react";
import { Row } from "react-bootstrap";

export const SelectionTile = ({ tileId, tileLabel, handleTileClick, tileChecked }) => {
  return (
    <Row >

    </Row>
  );
};
